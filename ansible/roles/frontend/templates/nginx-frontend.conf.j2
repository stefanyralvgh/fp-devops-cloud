server {
    listen 80 default_server;
    server_name _;

    root /opt/movie-analyst-ui;
    index index.html;

    # Redirect /authors directly to backend /reviewers (bypass ALB path rules)
    location = /authors {
        proxy_pass http://{{ alb_dns_name }}/reviewers;
        proxy_http_version 1.1;
        proxy_set_header Host {{ alb_dns_name }};  # ⬅️ CAMBIO CLAVE
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API calls
    location /api/ {
        proxy_pass http://{{ alb_dns_name }}/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files
    location / {
        try_files $uri $uri/ /index.html;
    }
}