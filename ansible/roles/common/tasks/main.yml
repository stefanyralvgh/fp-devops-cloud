---
# Common role - System baseline configuration
# This role prepares all instances with baseline software and settings

- name: Update all dnfs to latest version
  dnf:
    name: '*'
    state: latest
    update_cache: yes
  tags: dnfs

- name: Install base development tools
  dnf:
    name:
      - git
      - wget
      - curl
      - vim
      - unzip
      - tar
      - tree
    state: present
  tags: dnfs

- name: Install Python 3 and pip
  dnf:
    name:
      - python3
      - python3-pip
    state: present
  tags: dnfs

- name: Set timezone to America/Bogota
  timezone:
    name: America/Bogota
  tags: system

- name: Ensure NTP is installed and running
  systemd:
    name: chronyd
    state: started
    enabled: yes
  tags: system